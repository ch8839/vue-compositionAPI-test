// 对 GCMRNTextView 的简单封装
import React from 'react'
import { View, Text, ViewStyle } from '@mrn/react-native'
import { GCMRNTextView } from '@mrn/mrn-module-component'
import { observer } from '@nibfe/tra-app-platform-core'

// export const OrderLabel = observer((props) => {

//   return (<GCMRNTextView style={{marginBottom: 4}} key={index} textModelList={content || []} />)
// })
