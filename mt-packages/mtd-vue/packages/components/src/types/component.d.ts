import {
  // ComponentOptionsBase,
  ComponentPublicInstance,
  ComponentInstance,
} from '@ss/mtd-adapter'

export type Component = ComponentPublicInstance;
export type CPI = ComponentInstance;

export type MTDUIComponentSize = 'small' | '' | 'large';
