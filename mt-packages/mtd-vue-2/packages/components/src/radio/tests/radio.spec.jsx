import Radio from '@components/radio'
import {
  createDemoTest,
  mount,
  triggerClick,
  wait,
  waitImmediate,
} from '@tests/utils';

describe('Radio', function () {
  createDemoTest('radio')
})
